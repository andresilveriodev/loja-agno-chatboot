PORT=3001
NODE_ENV=development
# Caminho do arquivo SQLite (padrão: data/loja.db)
# DATABASE_PATH=data/loja.db
# URL do ai-service (AGNO). Se vazio, o chat usa resposta de fallback.
# AI_SERVICE_URL=http://localhost:8000
# Origens CORS separadas por vírgula (padrão: localhost:3000,3001,3002)
# CORS_ORIGIN=http://localhost:3000,http://localhost:3001,http://localhost:3002

# Fase 6: WhatsApp (Evolution API)
# URL da Evolution API (este projeto usa porta 8081 no docker-compose para não conflitar com 8080)
# EVOLUTION_API_URL=http://localhost:8081
# Chave de autenticação (mesma do .env do docker-compose ou da Evolution)
# EVOLUTION_API_KEY=change-me
# Nome da instância criada na Evolution (ex: loja). Preferir sem espaços.
# EVOLUTION_INSTANCE_NAME=loja
# Webhook: na Evolution, configurar URL = http://host.docker.internal:3001/api/whatsapp/webhook (backend no host)
